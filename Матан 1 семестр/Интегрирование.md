**Опр**
Функция $F(x)$ называется первообразной функции $f(x)$ , если
$$dF(x) = f(x)dx,\ то\ есть f(x) = F'(x)$$
## *Th*
Если $F_1(x)$ и $F_2(x)$ - первообразные функции $f(x)$ тогда $F_1(x) - F_2(x) = const$ 
**Опр**
Интегрирование - операция отыскания первообразной
$$\int f(x)dx = F(x) + C$$

Дифференциал от интеграла равен подынтегральной функции
$$d\int f(x)dx = dF(x) = f(x)dx$$
## *Th Свойства интегрирования*

1) Линейность
$$\forall A,B - const; \int(A*f(x) + B*g(x))dx = A\int f(x) + B * \int g(x)$$
2) [[Интегрирование по частям]]

3) [[Интегрирование путем замены переменной]]

См также:
[[Интегрирование тригонометрических функций]]
